fastapi>=0.111
uvicorn[standard]>=0.30
sqlalchemy>=2.0
alembic>=1.13
psycopg2-binary>=2.9
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0
websockets>=12.0

# Reused from ../requirements.txt so inference reproduces training exactly.
# scikit-learn is pinned (not >=) to match the version the pickled
# IsolationForest/StandardScaler artifacts under models/ were actually
# trained with -- unpickling across a scikit-learn version jump is a known
# way to get silently wrong results, not just a warning.
pandas>=2.2
numpy>=1.26
scikit-learn==1.6.1
xgboost>=2.1
torch>=2.3
shap>=0.45
joblib>=1.4

pytest>=8.2
httpx>=0.27

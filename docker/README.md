# docker

Containerisation for the production stack. Not yet implemented.

Planned:

- `backend.Dockerfile` — FastAPI inference service.
- `frontend.Dockerfile` — Next.js dashboard.
- `docker-compose.yml` — backend, frontend, Postgres, Redis for local development.

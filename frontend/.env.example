# Copy to .env.local for dev. docker-compose overrides these to point at the
# backend service's container-network address instead of localhost.
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws/live
